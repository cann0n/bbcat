<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="AnimationPreview">
        <item name="android:windowEnterAnimation">@anim/fade_in</item>
        <item name="android:windowExitAnimation">@anim/fade_out</item>
    </style>

    <style name="BabyInfosDialogAnim" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/dialoganim_enter</item>
        <item name="android:windowExitAnimation">@anim/dialoganim_exit</item>
    </style>

    <style name="PhotoCameraDialogAnim" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/dialoganim_enter</item>
        <item name="android:windowExitAnimation">@anim/dialoganim_exit</item>
    </style>

</resources>
